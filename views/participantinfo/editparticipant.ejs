<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit User - Participant Database</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.0/font/bootstrap-icons.css">
    <link rel="stylesheet" href="/styles.css">
</head>

<body class="bg-light custom-bg">

    <nav class="navbar navbar-expand-lg er-nav">
        <div class="container d-flex justify-content-between align-items-center">

            <!-- Logo -->
            <a class="navbar-brand d-flex align-items-center" href="/">
                <img src="/images/ella.jpeg" class="er-logo-small me-2">
                <span class="fw-bold">Ella Rises</span>
            </a>

            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navMenu">
                <span class="navbar-toggler-icon"></span>
            </button>

            <div class="collapse navbar-collapse" id="navMenu">
                <ul class="navbar-nav ms-auto align-items-center gap-2">

                    <% if (!user) { %>
                        <li class="nav-item">
                            <a class="nav-link er-donate-btn" href="/login">Login</a>
                        </li>
                    <% } else { %>
                        <li class="nav-item"><a class="nav-link nav-btn" href="/surveys">Surveys</a></li>
                        <li class="nav-item"><a class="nav-link nav-btn" href="/milestones">Milestones</a></li>
                        <li class="nav-item"><a class="nav-link nav-btn" href="/donations">Donations</a></li>
                        <li class="nav-item"><a class="nav-link nav-btn" href="/events">Events</a></li>

                        <% if (user.role === "admin") { %>
                            <li class="nav-item"><a class="nav-link nav-btn" href="/participants">Participants</a></li>
                            <li class="nav-item"><a class="nav-link nav-btn" href="/dashboard">Dashboard</a></li>
                        <% } %>

                        <li class="nav-item">
                            <a class="nav-link nav-btn logout-btn" href="/logout">Logout</a>
                        </li>
                    <% } %>
                </ul>
            </div>

        </div>
    </nav>

    <div class="container py-4">

        <div class="row justify-content-center">
            <div class="col-lg-7 col-md-9">

                <div class="card shadow border-0">
                    <div class="card-header bg-primary custom-nav-color text-white">
                        <h4 class="mb-0 text-center"><i class="bi bi-pencil"></i> Edit User Information</h4>
                    </div>

                    <div class="card-body p-4">

                        <form action="/update/<%= participant.participant_id %>" method="POST">

                            <!-- Participant ID -->
                            <div class="mb-3">
                                <label class="form-label fw-semibold">Participant ID</label>
                                <input type="text" class="form-control" value="<%= participant.participant_id %>" disabled>
                            </div>

                            <!-- First Name -->
                            <div class="mb-3">
                                <label class="form-label fw-semibold">First Name</label>
                                <input type="text" name="first_name" class="form-control" value="<%= participant.first_name %>" required>
                            </div>

                            <!-- Last Name -->
                            <div class="mb-3">
                                <label class="form-label fw-semibold">Last Name</label>
                                <input type="text" name="last_name" class="form-control" value="<%= participant.last_name %>" required>
                            </div>

                            <!-- Email -->
                            <div class="mb-3">
                                <label class="form-label fw-semibold">Email</label>
                                <input type="email" name="email" class="form-control" value="<%= participant.email %>" required>
                            </div>

                            <!-- Password (Optional) -->
                            <div class="mb-3">
                                <label class="form-label fw-semibold">New Password (optional)</label>
                                <input 
                                    type="password" 
                                    name="password" 
                                    placeholder="Leave blank to keep current password"
                                    class="form-control">
                            </div>

                            <!-- DOB -->
                            <div class="mb-3">
                                <label class="form-label fw-semibold">Date of Birth</label>
                                <input 
                                    type="date" 
                                    name="dob" 
                                    class="form-control" 
                                    value="<%= participant.dob.toISOString().split('T')[0] %>" 
                                    required>
                            </div>

                            <!-- Role -->
                            <div class="mb-3">
                                <label class="form-label fw-semibold">Role</label>
                                <select name="role" class="form-select" required>
                                    <option value="Student" <%= user.role === 'Student' ? 'selected' : '' %>>Student</option>
                                    <option value="Professional" <%= user.role === 'Professional' ? 'selected' : '' %>>Professional</option>
                                    <option value="admin" <%= user.role === 'admin' ? 'selected' : '' %>>Admin</option>
                                    <option value="Other" <%= user.role === 'Other' ? 'selected' : '' %>>Other</option>
                                </select>
                            </div>

                            <!-- Phone -->
                            <div class="mb-3">
                                <label class="form-label fw-semibold">Phone</label>
                                <input type="text" name="phone" class="form-control" value="<%= participant.phone %>" required>
                            </div>

                            <!-- City -->
                            <div class="mb-3">
                                <label class="form-label fw-semibold">City</label>
                                <input type="text" name="city" class="form-control" value="<%= participant.city %>" required>
                            </div>

                            <!-- State -->
                            <div class="mb-3">
                                <label class="form-label fw-semibold">State</label>
                                <input type="text" name="state" class="form-control" value="<%= participant.state %>" required>
                            </div>

                            <!-- Zip Code -->
                            <div class="mb-3">
                                <label class="form-label fw-semibold">Zip Code</label>
                                <input type="text" name="zip" class="form-control" value="<%= participant.zip %>" required>
                            </div>

                            <!-- School / Employer -->
                            <div class="mb-3">
                                <label class="form-label fw-semibold">School / Employer</label>
                                <input type="text" name="school_or_employer" class="form-control" value="<%= participant.school_or_employer %>" required>
                            </div>

                            <!-- Field of Interest -->
                            <div class="mb-3">
                                <label class="form-label fw-semibold">Field of Interest</label>
                                <select name="field_of_interest" class="form-select" required>
                                    <option value="STEM" <%= participant.field_of_interest === 'STEM' ? 'selected' : '' %>>STEM</option>
                                    <option value="Arts" <%= participant.field_of_interest === 'Arts' ? 'selected' : '' %>>Arts</option>
                                    <option value="Both" <%= participant.field_of_interest === 'Both' ? 'selected' : '' %>>Both</option>
                                </select>
                            </div>

                            <!-- Buttons -->
                            <div class="text-center mt-4">
                                <a href="/participants" class="btn btn-secondary ms-2 px-4">
                                    <i class="bi bi-arrow-left-circle"></i> Cancel
                                <button class="btn btn-success px-4" type="submit">
                                    <i class="bi bi-save"></i> Save Changes
                                </button>
                                </a>
                            </div>

                        </form>

                    </div>
                </div>

            </div>
        </div>

    </div>

</body>
</html>
